buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.7.+'
    }
}
apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
        // not working
        resources {
            srcDir 'src/org/wikimedia/morelangs/res'
        }
    }
}